# 発送番モード（ラベル購入まで自動化）

## 起動と終了
- 開始: ユーザーが「**発送番よろしく**」
- 終了: ユーザーが「**もういいよ**」
- 前提: PCが起きていて Claude が動いていること（Plus Shippingは
  Shopify管理画面のiframe内でしか動かず、公開APIも無いためクラウド無人化は不可）

## 1巡の手順（30分ごと）
1. `python ship_check.py` を実行
   - 新規0件 → 何もせず次の巡回へ（静かに待つ）
2. `自動購入OK` の注文について、1件ずつ:
   - Plus Shipping の一覧 → 該当行 → 詳細ページを開く
   - **エラーバナーが出ていたら中止してユーザーに報告**（ここが最終ゲート）
   - プリセット「コンパクト標準」→ 適用
   - 「送り状の支払いへ進む」→ 確認ダイアログ →「配送料を支払う」
   - 「送り状PDFを発行する」（有効300秒。再ダウンロードは60秒なので初回で取る）
   - PDFを curl で取得 → pypdfium2で300dpi画像化 → Pillowで画像PDF化
   - `python post_chat.py file <PDF> @<メッセージファイル>` で家族ルームへ投稿
   - `python ship_check.py --mark #1110` で処理済みに記録
   - ユーザーへ「#1110 購入しました ¥825 追跡xxxx」と報告
3. `要確認` の注文 → 購入せずユーザーに内容と理由を提示して指示を待つ

## 自動購入の境界（ship_check.py が判定）
| 条件 | 動作 |
|---|---|
| ライン中心・2点以下・竿なし・住所OK | 🟢 自動購入 |
| 竿(NES 8100等)を含む | 🔴 確認（2個口・サイズ判断が必要） |
| 3点以上 | 🔴 確認 |
| 町域にアラビア数字(例:神楽5条)で市区町村欄に無い | 🔴 確認（ヤマトが弾く） |
| 住所の並びが郵便番号と不一致 / 二重入力 | 🔴 確認 |
| 郵便番号を照合できない | 🔴 確認 |

`AUTO_LIMIT = 1000`（円）。想定を超える送料が出たら購入せず報告すること。

## 注意点
- 明細表は autoflow.py がクラウドで自動投稿済み。ここでやるのは**送り状のみ**
- ヤマトのメンテ時間（毎日1:00〜7:00）は購入・印刷不可 → その間は巡回を休む
- Chatworkへの投稿は必ず `post_chat.py` 経由（シェル引数で日本語を渡すと文字化けする）
- ブラウザが固まったら新規タブを作り直す。頻発するならメモリ逼迫なので報告する
